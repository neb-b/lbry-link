{
  "name": "LBRY Link",
  "version": "0.0.3",
  "description": "Finds LBRY URLs on a page and adds a link tag that opens the LBRY desktop app",
  "manifest_version": 2,
  "content_scripts": [
    {
      "matches": ["<all_urls>"],
      "run_at": "document_end",
      "js": ["content.js"]
    }
  ]
}
