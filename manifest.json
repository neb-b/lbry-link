{
  "name": "LBRY Link",
  "version": "0.0.4",
  "description": "Finds LBRY URLs on a page and adds a link tag that opens the LBRY desktop app",
  "manifest_version": 2,
  "content_scripts": [
    {
      "matches": ["*://*.github.com/*"],
      "run_at": "document_end",
      "js": ["content.js"]
    }
  ]
}
